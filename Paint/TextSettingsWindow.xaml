<Window x:Class="Paint.TextSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paint"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Text" Height="150" Width="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="0.6*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Input Text:" VerticalAlignment="Center"
               HorizontalAlignment="Right"/>
        <TextBox Grid.Row="0" Grid.Column="1"
                 VerticalAlignment="Center"
                 Margin="2"
                 x:Name="inputTextBox"/>
        <Button Grid.Row="1" Grid.ColumnSpan="2" Content="Font settings" VerticalAlignment="Center" Margin="2" x:Name="fontSettingsButton"
                Click="fontSettingsButton_Click"/>
        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Accept" VerticalAlignment="Center" Margin="2" x:Name="acceptSettingsButton"
                Click="acceptSettingsButton_Click"/>
    </Grid>
</Window>
